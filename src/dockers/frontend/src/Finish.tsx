import React from 'react';
import { useNavigate } from 'react-router-dom';


const Finish: React.FC = () => {
    const navigate = useNavigate();

  return (    
    <h2>Pantalla Que Muestra al Vencedor</h2>
  );
};

export default Finish;